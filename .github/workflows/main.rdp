            - name: Setup Windows App Developer CLI
  uses: microsoft/setup-WinAppCli@v0.1
  with:
    # The version of the Windows App Developer CLI to be downloaded and configured (e.g. 'v0.1.0').
    version: # optional, default is latest
          
